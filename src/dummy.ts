import { ThingBuilder } from "./ThingBuilder";

class Test{
    run(){
        // ThingBuilder.buildFromFile("Test","Mein Test","./assets/proxon-registers.json",(name:string,value:any)=>{console.log(value)});
        
    }
}
new Test().run();